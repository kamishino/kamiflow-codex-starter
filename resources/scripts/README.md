# Scripts Placeholder

SSOT script specs may be documented here.

Executable helper scripts currently live under root `scripts/`, including:

- `scripts/portability/smoke-one-repo.ts` (wired via `npm run portability:smoke`)
