Use the installed `kamiflow-core` skill at `{{SKILL_PATH}}`.

There is already an active plan in `{{PROJECT_DIR}}`. Open plan view. Keep this operational: do not implement, validate, archive, or rewrite the plan.
