Use the installed `kamiflow-core` skill at `{{SKILL_PATH}}`.

Please jump straight into implementing the division fix in `calc.js`. Work directly in `{{PROJECT_DIR}}`, run any commands you need yourself, and tell me whether the slice passes.
