Use the installed `kamiflow-core` skill at `{{SKILL_PATH}}`.

The project brief already exists at `{{PROJECT_DIR}}/.local/project.md`. Plan the next feature slice for cross-device note sync. Keep the plan aligned with the existing project brief and do not implement code.
