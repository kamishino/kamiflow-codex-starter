Use the installed `kamiflow-core` skill at `{{SKILL_PATH}}`.

The implementation should already be complete. Work directly in `{{PROJECT_DIR}}`, verify the current behavior against the active plan, run the validation command yourself, and close the plan only if the evidence supports PASS.
