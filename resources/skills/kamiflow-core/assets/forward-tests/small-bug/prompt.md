Use the installed `kamiflow-core` skill at `{{SKILL_PATH}}`.

Please fix the small bug in `math.js`: `add(-2, 1)` returns the wrong result. Work directly in `{{PROJECT_DIR}}`, repair the smallest useful slice, run the validation command yourself, update the active plan, and tell me whether the slice passes.
