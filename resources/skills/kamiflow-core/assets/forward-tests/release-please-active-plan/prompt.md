Use the installed `kamiflow-core` skill at `{{SKILL_PATH}}`.

There is already an active PASS plan in `{{PROJECT_DIR}}` and the functional work is already committed. Release please. Keep this operational and use the existing finish guidance instead of reopening planning.
