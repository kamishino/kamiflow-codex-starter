Use the installed `kamiflow-core` skill at `{{SKILL_PATH}}`.

I want a tiny terminal notes helper in `{{PROJECT_DIR}}`. The idea is small, but I have not chosen the first slice yet. It might focus on quick capture, tags, or search, and I need the next implementation slice clarified before planning or coding.

Work directly in `{{PROJECT_DIR}}`. Recover or create the active plan if needed, choose the right first route, persist the shaping work, and leave me with one concrete next step. Run any commands you need yourself. Do not invent implementation status or claim something is done without evidence.
