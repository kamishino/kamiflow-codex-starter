Use the installed `kamiflow-core` skill at `{{SKILL_PATH}}`.

Please investigate why mixed-sign addition is wrong in `math.js`. Do not implement yet. Work directly in `{{PROJECT_DIR}}`, compare the most likely causes, keep implementation files unchanged, and recommend the next route from evidence.
