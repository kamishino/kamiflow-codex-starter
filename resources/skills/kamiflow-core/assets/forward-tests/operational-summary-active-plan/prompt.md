Use the installed `kamiflow-core` skill at `{{SKILL_PATH}}`.

There is already an active plan in `{{PROJECT_DIR}}`. Give me a short status summary of the current slice and the next step only. Do not implement, validate, archive, or rewrite the plan.
