# Dogfooding profile: optimize for rapid local iteration.
# Anti-pattern reference: AP-001 (repo-context commands allowed here by design).

prefix_rule(
  decision = "allow",
  pattern = ["npm", "run", "dogfood:link"],
  match = ["npm run dogfood:link"],
  not_match = ["npm run dogfood:smoke"]
)

prefix_rule(
  decision = "allow",
  pattern = ["npm", "run", "dogfood:smoke"],
  match = ["npm run dogfood:smoke"],
  not_match = ["npm run dogfood:link"]
)

prefix_rule(
  decision = "allow",
  pattern = ["npm", "run", "dogfood:pack-smoke"],
  match = ["npm run dogfood:pack-smoke"],
  not_match = ["npm run dogfood:smoke"]
)

prefix_rule(
  decision = "allow",
  pattern = ["npm", "run", "portability:smoke"],
  match = ["npm run portability:smoke -- --project dogfood/realistic-workflow --link"],
  not_match = ["npm run dogfood:smoke"]
)

prefix_rule(
  decision = "allow",
  pattern = ["npm", "install"],
  match = ["npm install"],
  not_match = ["npm run dogfood:link"]
)

prefix_rule(
  decision = "allow",
  pattern = ["npm", "run", "-w", "@kamishino/kfc-plan-web", "build"],
  match = ["npm run -w @kamishino/kfc-plan-web build"],
  not_match = ["npm install"]
)

prefix_rule(
  decision = "allow",
  pattern = ["npm", "pack", "--silent"],
  match = ["npm pack --silent"],
  not_match = ["npm pack"]
)

