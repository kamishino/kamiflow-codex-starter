# minimal-js

Small JavaScript consumer fixture for dogfooding.

## Link flow

1. Run from repo root: `npm run dogfood:link`
2. Run inside this fixture: `npm run doctor` then `npm run run`

## Tarball flow

Run from repo root: `npm run dogfood:pack-smoke`
