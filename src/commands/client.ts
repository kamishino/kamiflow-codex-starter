export { runClient } from "./surface/client.js";

/**
 * Client AGENTS.md scaffolded:
 * Client AGENTS.md managed block inserted:
 * KFC:BEGIN MANAGED
 * /init`-equivalent contract
 * .kfc/CODEX_READY.md
 * .kfc/LESSONS.md
 * .agents/skills/kamiflow-core/SKILL.md
 * ## Workflow Commands
 * kfc client done
 * `kfc client status`
 * autonomous
 * CLIENT_AGENTS_SHARED_CONTRACT_FILE
 * resolveClientAgentsSharedContractPath(projectDir)
 * buildClientAgentsManagedBlock(projectDir)
 * fsp.readFile(sharedContractPath)
 * evaluateClientSetupCompletion(projectDir, planId)
 * Check: PASS|BLOCK
 * Inspection Status: ${summary.inspectionStatus}
 * Client Status: ${summary.status}
 * Onboarding Status: BLOCK
 * Error Code: CLIENT_SETUP_BLOCK
 * smart recovery cycle
 * Project-local skill synced:
 * Client lessons scaffolded:
 * Private ignore entries already present:
 * Prepended private ignore entries:
 */
